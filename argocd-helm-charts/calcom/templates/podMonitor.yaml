{{- if .Values.postgresql.monitoring.enablePodMonitor 
}}
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: {{ if .Values.postgresql.recover }}calcom-pgsql-recover{{ else }}calcom-pgsql{{ end }}
  namespace: {{ .Release.Namespace }}
spec:
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
  selector:
    matchLabels:
      cnpg.io/cluster: {{ if .Values.postgresql.recover }}calcom-pgsql-recover{{ else }}calcom-pgsql{{ end }}
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
      interval: 10s
      scrapeTimeout: 10s
{{- end }}